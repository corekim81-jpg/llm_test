# ================================================================
# monitoring_llm 의존성
# ================================================================
# 설치:
#   pip install -r requirements.txt
# ================================================================

# ── FastAPI 서버 ──────────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.46.0
sse-starlette==3.4.2       # SSE 스트리밍
pydantic==2.13.4

# ── LangChain / LangGraph ────────────────────────────────────
langchain-core==1.3.3
langchain-ollama==1.1.0    # Ollama (Qwen3:8b 등 로컬 LLM)
langchain-anthropic>=0.3.0 # Anthropic Claude (LLM_PROVIDER=anthropic 시 필요)
langgraph==1.1.10

# ── HTTP 클라이언트 ───────────────────────────────────────────
requests==2.33.1
httpx==0.28.1                  # 비동기 HTTP (routes.py 헬스체크)
aiofiles==24.1.0               # StaticFiles 서빙 필수

# ── 환경변수 / 시간 파싱 ──────────────────────────────────────
python-dotenv==1.2.2
pendulum==3.2.0             # 한국어 시간 표현 파싱

# ── BERT 파인튜닝 (LLM_PROVIDER=bert-local 또는 BERT_MODEL_PATH 설정 시 필요) ──
# pip install -r requirements-bert.txt 로 별도 설치 권장 (torch 용량 큼)
# transformers>=4.40.0
# torch>=2.0.0
# scikit-learn>=1.3.0

# ── RAG/pgvector (PGVECTOR_URL 설정 시 필요) ──────────────────
# pip install -r requirements-rag.txt 로 별도 설치 권장
# asyncpg>=0.29.0
# apscheduler>=3.10.0
