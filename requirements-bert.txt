# ================================================================
# BERT 파인튜닝 전용 의존성
# ================================================================
# 설치:
#   pip install -r requirements-bert.txt
#
# GPU (RTX 3070 등) 사용 시 CUDA 버전에 맞는 torch 설치:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ================================================================

transformers>=4.40.0
torch>=2.0.0
scikit-learn>=1.3.0
accelerate>=0.26.0   # Trainer fp16 지원
